$(function () {
  var options = {
    srcNode: 'img',
    margin: '15px',
    width: '240px',
    max_width: '',
    resizable: true,
    transition: 'all 0.5s ease',
  };
  $('.grid').gridify(options);
});
